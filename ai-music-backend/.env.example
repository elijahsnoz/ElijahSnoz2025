STORAGE_DIR=storage
MAX_FILE_SIZE_BYTES=62914560
DEMUCS_MODEL=htdemucs

# Comma-separated origins allowed to call this service directly from the
# browser (the /upload endpoint bypasses the Next.js proxy — see
# ai-music-lab/lib/upload.ts). Must include the production site's origin.
ALLOWED_ORIGINS=https://elijahsnoz.me,http://localhost:3000
